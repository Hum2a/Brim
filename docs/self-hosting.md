# Self-hosting Brim

Stub — fill in as the stack lands.

## Intended sections

1. Requirements (Node 20+, npm, optional Neon, optional Google Routes key)
2. Fixture mode (zero keys)
3. Environment variables and Wrangler secrets
4. Database (Neon + PostGIS + RLS migrations)
5. Routing provider choice (OSRM vs Google)
6. Fuel Finder / DVLA credentials
7. Deploying `apps/api` (Workers) and `apps/web` (Pages or Workers assets)
8. Cron ingestion worker
9. What you are responsible for (charge liability remains with the driver)
