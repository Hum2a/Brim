# Local / Neon branch. Copy to .env then `npm run env:sync`.
# Never commit real values. Staging and production use .env.staging / .env.production.

DATABASE_URL=
BETTER_AUTH_SECRET=
GOOGLE_MAPS_API_KEY=
FUEL_FINDER_CLIENT_ID=
FUEL_FINDER_CLIENT_SECRET=
DVLA_VES_API_KEY=
RESEND_API_KEY=
VRM_ENCRYPTION_KEY=
OSRM_URL=
BRIM_FIXTURES=0

# Leave empty locally — Vite proxies /v1 to wrangler on :8787.
VITE_API_BASE=
